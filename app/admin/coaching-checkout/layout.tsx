import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Admin - Coaching Checkout',
}

export default function AdminCoachingCheckoutLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
