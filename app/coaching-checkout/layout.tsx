import { Metadata } from 'next'

export const metadata: Metadata = {
  title: '1-1 Checkout | Oracle Boxing',
}

export default function CoachingCheckoutLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
